repo:
  scanPath: ./preview-repos
  readme:
    - readme
    - README
    - readme.md
    - README.md
  mainBranch:
    - master
    - main

meta: 
  title: git-preview's preview
  description: not a full git forge
  syntaxHighlight: monokailight

server:
  name: localhost
  host: 127.0.0.1
  port: 5555
